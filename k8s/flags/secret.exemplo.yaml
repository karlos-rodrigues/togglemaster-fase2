# MODELO — gerar no deploy, nunca versionar com valor real.
#   echo -n 'postgres://user:senha@endpoint-rds:5432/flag_db' | base64 -w0
apiVersion: v1
kind: Secret
metadata: { name: flag-service-secrets, namespace: flags }
type: Opaque
data:
  DATABASE_URL: BASE64_DA_CONNECTION_STRING_DO_RDS
