apiVersion: apps/v1
kind: Deployment
metadata:
  name: flag-service
  namespace: flags
  labels: { app: flag-service, app.kubernetes.io/part-of: togglemaster }
spec:
  replicas: 2
  selector: { matchLabels: { app: flag-service } }
  template:
    metadata:
      labels: { app: flag-service }
    spec:
      containers:
        - name: flag-service
          image: 217579643777.dkr.ecr.us-east-1.amazonaws.com/flag-service:v1
          ports: [ { containerPort: 8002, name: http } ]
          envFrom:
            - configMapRef: { name: flag-service-config }
          env:
            - name: DATABASE_URL
              valueFrom: { secretKeyRef: { name: flag-service-secrets, key: DATABASE_URL } }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 500m, memory: 384Mi }
          # Python sob Gunicorn demora mais para aceitar tráfego que um binário Go.
          readinessProbe:
            httpGet: { path: /health, port: http }
            initialDelaySeconds: 15
            periodSeconds: 5
          livenessProbe:
            httpGet: { path: /health, port: http }
            initialDelaySeconds: 30
            periodSeconds: 15
