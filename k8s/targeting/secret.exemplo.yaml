# MODELO — gerar no deploy, nunca versionar com valor real.
#   echo -n 'postgres://user:senha@endpoint-rds:5432/targeting_db' | base64 -w0
apiVersion: v1
kind: Secret
metadata: { name: targeting-service-secrets, namespace: targeting }
type: Opaque
data:
  DATABASE_URL: BASE64_DA_CONNECTION_STRING_DO_RDS
